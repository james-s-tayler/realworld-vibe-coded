---
description: Verify backend formatting & analyzers (no changes). Fails if issues found
---

Run `./build.sh LintServerVerify`. Check output for details. If failures occur, check `Reports/` and `Logs/` directories.
