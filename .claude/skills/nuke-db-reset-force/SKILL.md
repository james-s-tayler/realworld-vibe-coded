---
description: Reset local dev database without confirmation by removing the docker volume
---

Run `./build.sh DbResetForce`. Check output for details. If failures occur, check `Reports/` and `Logs/` directories.
