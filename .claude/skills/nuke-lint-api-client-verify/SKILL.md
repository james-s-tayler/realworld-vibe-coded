---
description: Verify generated API client is in sync with server endpoints
---

Run `./build.sh LintApiClientVerify`. Check output for details. If failures occur, check `Reports/` and `Logs/` directories.
