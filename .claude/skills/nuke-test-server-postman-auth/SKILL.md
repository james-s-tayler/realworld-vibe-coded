---
description: Run postman tests for Auth collection using Docker Compose
---

Run `./build.sh TestServerPostmanAuth`. Check output for details. If failures occur, check `Reports/` and `Logs/` directories.
