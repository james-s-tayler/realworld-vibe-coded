---
description: Reset local SQL Server database by removing docker volume (confirm or --force to skip)
---

Run `./build.sh DbReset`. Check output for details. If failures occur, check `Reports/` and `Logs/` directories.
