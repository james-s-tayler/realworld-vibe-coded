---
description: Verify Nuke build targets for documentation and naming conventions
---

Run `./build.sh LintNukeVerify`. Check output for details. If failures occur, check `Reports/` and `Logs/` directories.
