---
description: Generate TypeScript API client from FastEndpoints using Kiota
---

Run `./build.sh BuildGenerateApiClient`. Check output for details. If failures occur, check `Reports/` and `Logs/` directories.
