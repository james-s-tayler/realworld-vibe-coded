---
description: Verify all .claude/rules/*.md files are within the 85-line limit
---

Run `./build.sh LintClaudeRulesVerify`. Check output for details. If failures occur, check `Reports/` and `Logs/` directories.
