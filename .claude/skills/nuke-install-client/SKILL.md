---
description: Run npm ci if node_modules is missing or package-lock.json is newer
---

Run `./build.sh InstallClient`. Check output for details. If failures occur, check `Reports/` and `Logs/` directories.
