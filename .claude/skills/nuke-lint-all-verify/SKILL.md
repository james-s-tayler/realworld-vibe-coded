---
description: Verify all C# code formatting & analyzers (no changes). Fails if issues found
---

Run `./build.sh LintAllVerify`. Check output for details. If failures occur, check `Reports/` and `Logs/` directories.
