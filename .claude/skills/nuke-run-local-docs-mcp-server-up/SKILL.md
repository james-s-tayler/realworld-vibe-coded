---
description: Start Docs MCP Server and ngrok in the background
---

Run `./build.sh RunLocalDocsMcpServerUp`. Check output for details. If failures occur, check `Reports/` and `Logs/` directories.
