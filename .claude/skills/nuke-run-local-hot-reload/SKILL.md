---
description: Run backend locally using Docker Compose with SQL Server and hot-reload
---

Run `./build.sh RunLocalHotReload`. Check output for details. If failures occur, check `Reports/` and `Logs/` directories.
