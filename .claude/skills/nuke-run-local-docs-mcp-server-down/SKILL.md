---
description: Stop Docs MCP Server and ngrok background processes
---

Run `./build.sh RunLocalDocsMcpServerDown`. Check output for details. If failures occur, check `Reports/` and `Logs/` directories.
