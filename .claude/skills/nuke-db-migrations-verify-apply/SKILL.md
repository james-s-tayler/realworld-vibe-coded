---
description: Verify EF Core migrations by applying them to a throwaway SQL Server database in Docker
---

Run `./build.sh DbMigrationsVerifyApply`. Check output for details. If failures occur, check `Reports/` and `Logs/` directories.
