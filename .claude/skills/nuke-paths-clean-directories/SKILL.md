---
description: Pre-create directories that Docker containers need to prevent root permission issues
---

Run `./build.sh PathsCleanDirectories`. Check output for details. If failures occur, check `Reports/` and `Logs/` directories.
