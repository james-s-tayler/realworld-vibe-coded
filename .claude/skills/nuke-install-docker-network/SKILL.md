---
description: Creates app-network docker network which is a shared global network
---

Run `./build.sh InstallDockerNetwork`. Check output for details. If failures occur, check `Reports/` and `Logs/` directories.
