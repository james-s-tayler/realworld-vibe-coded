---
description: Run E2E Playwright tests for Articles tier (EditorPage, ArticlePage, ProfilePage)
---

Run `./build.sh TestE2eArticles`. Check output for details. If failures occur, check `Reports/` and `Logs/` directories.
