---
description: Run backend locally using Docker Compose with published artifact
---

Run `./build.sh RunLocalPublish`. Check output for details. If failures occur, check `Reports/` and `Logs/` directories.
