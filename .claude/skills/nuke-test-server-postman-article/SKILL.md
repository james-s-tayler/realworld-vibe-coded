---
description: Run postman tests for Article collection using Docker Compose
---

Run `./build.sh TestServerPostmanArticle`. Check output for details. If failures occur, check `Reports/` and `Logs/` directories.
