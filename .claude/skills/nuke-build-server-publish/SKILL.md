---
description: Publish App/Server for linux-x64 in Release configuration with App/Client/dist in wwwroot
---

Run `./build.sh BuildServerPublish`. Check output for details. If failures occur, check `Reports/` and `Logs/` directories.
