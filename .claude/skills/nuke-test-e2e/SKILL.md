---
description: Run Playwright end-to-end tests. Use after changes that affect user-facing flows or when both frontend and backend changes need integration validation.
---

Run `./build.sh TestE2e`. If tests fail, check `Reports/` for Playwright traces and screenshots. Follow any instructions in the error messages for accessing specific reports.
