---
description: Verify every Nuke target has a corresponding Claude Code skill
---

Run `./build.sh LintSkillsVerify`. Check output for details. If failures occur, check `Reports/` and `Logs/` directories.
